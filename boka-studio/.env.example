# Supabase — email OTP auth (required for real verification emails)
# Get anon key: Project Settings → API → anon public
VITE_SUPABASE_URL=https://qmzjdtkdzmkcrmtulwuv.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# BokaMarket ASP.NET API (products, orders, yarn stock, custom orders)
# Render free tier sleeps — use local API for faster dev:
# VITE_API_URL=https://localhost:7123
VITE_API_URL=https://boka-market-backend.onrender.com

# Required for admin registration (customer registration is open).
VITE_ADMIN_INVITE_CODE=boka-admin-2026
